import HeroContainer from "@/feature/beranda/hero/container/HeroContainer";

const page = () => {
  return (
    <main className="w-full">
      <HeroContainer />
    </main>
  );
};

export default page;
